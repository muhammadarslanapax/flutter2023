 intl: ^0.18.0
 import 'package:intl/intl.dart';
   var hours = DateTime.now().hour;
    var dt = DateTime.now();
    var mints= DateTime.now().minute;
    var second = DateTime.now().second;

    var day = DateTime.now().day;
    var month = DateTime.now().month;
    var yera = DateTime.now().year;
    
      child: Text(" ${DateFormat('Hms').format(dt)}",style: TextStyle(fontSize: 35),),),
      
      
      
      
      ElevatedButton(onPressed: () async {
            // setState(() {
            //
            // });
            DateTime? date = await showDatePicker(context: context, initialDate: DateTime.now(), firstDate: DateTime(1900), lastDate: DateTime(2022));
            // TimeOfDay? pickedtime = await showTimePicker(context: context, initialTime: TimeOfDay.now(),
            // initialEntryMode: TimePickerEntryMode.dial);
            if(DateTime !=null){
            print(date.)
}




          }
          
          
          --------------------
            DateTime? date = await showDatePicker(
                context: context,
                initialDate:DateTime.now(),
                firstDate:DateTime(1900),
                lastDate: DateTime(2100));            // TimeOfDay? pickedtime = await showTimePicker(context: context, initialTime: TimeOfDay.now(),
            // initialEntryMode: TimePickerEntryMode.dial);
            
            
            if(DateTime!=null){
              setState(() {
                print(date?.year);
                
              });
            }
